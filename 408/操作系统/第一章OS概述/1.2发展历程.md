# 1.2发展历程
1. 手工操作
	1. 手工阶段
	2. 脱机IO：有专门一个外围机处理输入
2. 批处理阶段(操作系统开始出现)
	1. 单道批处理系统
		1. 自动性：磁带上的作业可以自动连续执行
		2. 顺序性：作业按照磁带的顺序装入内存
		3. 单道性：仅允许一道用户程序运行
		4. [[作业和程序的关系]]
	2. 多道批处理系统
			作业首先存放在外存的后备队列中。作业调度程序按算法选取若干作业调入内存，使其在管理程序的控制下并发执行，并共享系统资源。
			当某道程序因 I/O 请求而暂停运行时，CPU立即切换至另一就绪程序继续执行。
		1. 需要**中断**
			1. 这样当需要IO的时候，CPU不等着直接进行下面的操作
			2. **cpu利用率高**
			3. 吞吐大
			4. IO利用率高
			5. **IO和CPU是并行的**
		2. 多道：内存中同时驻留多道独立程序
		3. 宏观上并行
		4. 微观上串行
		5. **缺乏交互能力**
		6. 
3. 分时操作系统(公平)
		分时技术是指将处理器时间划分为很短的时间片，轮流分配给各用户程序使用。若某程序在其时间片内未完成计算，则暂停运行，释放处理器给其他用户程序，待下一轮再继续执行。
		**为了更快响应用户**
		**解决了交互问题**

	1. 同时性（多路性）。允许多个终端用户(**多用户**)同时使用同一台计算机。
	2. 交互性。用户可通过终端以人机对话方式直接控制程序运行，并与其程序进行交互。3.
	3. 独立性。各用户的操作相互隔离，任一用户均感觉系统为其独占。
	4. 及时性。用户请求能在较短时间内获得响应，满足交互需求。
4. 实时操作系统
		实时操作系统是为在严格的时间限制内完成关键任务而设计的系统。
		基于任务的紧迫性
5. 网络操作系统和分布式
6. 微机操作系统